query GetIndexes($after: String $first: Int) {
    indexes (after: $after first: $first) {
        pageInfo {
            hasNextPage
        }
    nodes {
        indexName,
        hight,
        last
        time
    }
  }
}